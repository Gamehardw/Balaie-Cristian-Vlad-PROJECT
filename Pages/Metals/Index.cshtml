@page
@model Balaie_Cristian_Vlad_PROJECT.Pages.Metals.IndexModel

@{
    ViewData["Title"] = "Metals";
}

<h1>Metals</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Metal[0].MetalName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Metal[0].Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Metal[0].Valency)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Metal) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MetalName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valency)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
